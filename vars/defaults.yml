---
# file: vars/Ubuntu.yml

build_dependencies:
  - make
  - pkg-config
  - check
  - g++
  - librsync-dev
  - libncurses5-dev
  - libz-dev
  - libssl-dev
  - uthash-dev
  - libyajl-dev
  - autoconf
  - automake
  - libtool
  - dh-autoreconf
  - libffi-dev  # required for burpui cffi

burp_agent_packages:
  - python-pip
  - python-dev
  - supervisor

